const activeUsers: string[] = [];
activeUsers.push('Tony');

const ageList: number[] = [12, 45, 65];
ageList[0] = 92;

// const bools: Array<boolean> = [];
const bools: boolean[] = [];

type XY = {
  x: number;
  y: number;
};

const coords: XY[] = [];
coords.push({ x: 23, y: 45 });

// 다차원 배열
const arrays: string[][] = [
  ['a', 'b', 'c'],
  ['d', 'e', 'f'],
];
